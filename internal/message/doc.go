// Package message accumulates SMTP message data and extracts bounded evidence
// for filtering. It owns MIME and transfer decoding, header normalization,
// HTML-to-text conversion, link extraction, and inline-image preparation.
package message
