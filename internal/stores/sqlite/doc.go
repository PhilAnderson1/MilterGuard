// Package sqlite implements the repository contracts in package stores using
// the shared SQLite connection owned by package sqlitedb. It contains
// application queries and maintenance policy, not database lifecycle setup.
package sqlite
