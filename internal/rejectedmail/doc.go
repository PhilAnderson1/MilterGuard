// Package rejectedmail stores bounded copies of rejected messages beneath a
// configured archive root. It also performs retention and target-size cleanup;
// rejection metadata remains in the repository layer.
package rejectedmail
